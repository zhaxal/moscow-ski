server_names_hash_bucket_size  1024;
# Cache zone
proxy_cache_path /var/cache/nginx levels=1:2 keys_zone=STATIC:10m inactive=7d use_temp_path=off;

server {
  listen 443 ssl default_server;
  listen [::]:443 ssl default_server;
  ssl_certificate /etc/nginx/certs/default.crt;
  ssl_certificate_key /etc/nginx/certs/default.key;
  return 444;
}

upstream ski {
  server ski_app:3000;
}

# ski.moscow.sport

server {
  listen 80;
  listen [::]:80;
  server_name ski.moscow.sport;
  return 301 https://ski.moscow.sport$request_uri;
}

server {
  listen 443 ssl;
  listen [::]:443 ssl;
  server_name ski.moscow.sport;
  ssl_certificate /etc/nginx/certs/www.ski.moscow.sport.crt;
  ssl_certificate_key /etc/nginx/certs/www.ski.moscow.sport.key;

  server_tokens off;
  
  gzip on;
  gzip_proxied any;
  gzip_comp_level 4;
  gzip_types text/css application/javascript image/svg+xml;

  proxy_http_version 1.1;
  proxy_set_header Upgrade $http_upgrade;
  proxy_set_header Connection 'upgrade';
  proxy_set_header Host $host;
  proxy_cache_bypass $http_upgrade;

  # BUILT ASSETS (E.G. JS BUNDLES)
  # Browser cache - max cache headers from Next.js as build id in url
  # Server cache - valid forever (cleared after cache "inactive" period)
  location /_next/static {
    proxy_cache STATIC;
    proxy_pass http://ski;
  }

  # STATIC ASSETS (E.G. IMAGES)
  # Browser cache - "no-cache" headers from Next.js as no build id in url
  # Server cache - refresh regularly in case of changes
  location /static {
    proxy_cache STATIC;
    proxy_ignore_headers Cache-Control;
    proxy_cache_valid 60m;
    proxy_pass http://ski;
  }

  # DYNAMIC ASSETS - NO CACHE
  location / {
    proxy_pass http://ski;
  }
}

upstream orient {
  server orient_app:3001;
}

# orient.moscow.sport

server {
  listen 80;
  listen [::]:80;
  server_name orient.moscow.sport;
  return 301 https://orient.moscow.sport$request_uri;
}

server {
  listen 443 ssl;
  listen [::]:443 ssl;
  server_name orient.moscow.sport;
  ssl_certificate /etc/nginx/certs/www.orient.moscow.sport.crt;
  ssl_certificate_key /etc/nginx/certs/www.orient.moscow.sport.key;

  server_tokens off;
  
  gzip on;
  gzip_proxied any;
  gzip_comp_level 4;
  gzip_types text/css application/javascript image/svg+xml;

  proxy_http_version 1.1;
  proxy_set_header Upgrade $http_upgrade;
  proxy_set_header Connection 'upgrade';
  proxy_set_header Host $host;
  proxy_cache_bypass $http_upgrade;

  # BUILT ASSETS (E.G. JS BUNDLES)
  # Browser cache - max cache headers from Next.js as build id in url
  # Server cache - valid forever (cleared after cache "inactive" period)
  location /_next/static {
    proxy_cache STATIC;
    proxy_pass http://orient;
  }

  # STATIC ASSETS (E.G. IMAGES)
  # Browser cache - "no-cache" headers from Next.js as no build id in url
  # Server cache - refresh regularly in case of changes
  location /static {
    proxy_cache STATIC;
    proxy_ignore_headers Cache-Control;
    proxy_cache_valid 60m;
    proxy_pass http://orient;
  }

  # DYNAMIC ASSETS - NO CACHE
  location / {
    proxy_pass http://orient;
  }
}

upstream led {
  server led_app:3002;
}

# led.moscow.sport

server {
  listen 80;
  listen [::]:80;
  server_name led.moscow.sport;
  return 301 https://led.moscow.sport$request_uri;
}

server {
  listen 443 ssl;
  listen [::]:443 ssl;
  server_name led.moscow.sport;
  ssl_certificate /etc/nginx/certs/www.led.moscow.sport.crt;
  ssl_certificate_key /etc/nginx/certs/www.led.moscow.sport.key;

  server_tokens off;
  
  gzip on;
  gzip_proxied any;
  gzip_comp_level 4;
  gzip_types text/css application/javascript image/svg+xml;

  proxy_http_version 1.1;
  proxy_set_header Upgrade $http_upgrade;
  proxy_set_header Connection 'upgrade';
  proxy_set_header Host $host;
  proxy_cache_bypass $http_upgrade;

  # BUILT ASSETS (E.G. JS BUNDLES)
  # Browser cache - max cache headers from Next.js as build id in url
  # Server cache - valid forever (cleared after cache "inactive" period)
  location /_next/static {
    proxy_cache STATIC;
    proxy_pass http://led;
  }

  # STATIC ASSETS (E.G. IMAGES)
  # Browser cache - "no-cache" headers from Next.js as no build id in url
  # Server cache - refresh regularly in case of changes
  location /static {
    proxy_cache STATIC;
    proxy_ignore_headers Cache-Control;
    proxy_cache_valid 60m;
    proxy_pass http://led;
  }

  # DYNAMIC ASSETS - NO CACHE
  location / {
    proxy_pass http://led;
  }
}

upstream pryzki {
  server pryzki_app:3003;
}

# pryzki.moscow.sport

server {
  listen 80;
  listen [::]:80;
  server_name pryzki.moscow.sport;
  return 301 https://pryzki.moscow.sport$request_uri;
}

server {
  listen 443 ssl;
  listen [::]:443 ssl;
  server_name pryzki.moscow.sport;
  ssl_certificate /etc/nginx/certs/www.pryzki.moscow.sport.crt;
  ssl_certificate_key /etc/nginx/certs/www.pryzki.moscow.sport.key;

  server_tokens off;
  
  gzip on;
  gzip_proxied any;
  gzip_comp_level 4;
  gzip_types text/css application/javascript image/svg+xml;

  proxy_http_version 1.1;
  proxy_set_header Upgrade $http_upgrade;
  proxy_set_header Connection 'upgrade';
  proxy_set_header Host $host;
  proxy_cache_bypass $http_upgrade;

  # BUILT ASSETS (E.G. JS BUNDLES)
  # Browser cache - max cache headers from Next.js as build id in url
  # Server cache - valid forever (cleared after cache "inactive" period)
  location /_next/static {
    proxy_cache STATIC;
    proxy_pass http://pryzki;
  }

  # STATIC ASSETS (E.G. IMAGES)
  # Browser cache - "no-cache" headers from Next.js as no build id in url
  # Server cache - refresh regularly in case of changes
  location /static {
    proxy_cache STATIC;
    proxy_ignore_headers Cache-Control;
    proxy_cache_valid 60m;
    proxy_pass http://pryzki;
  }

  # DYNAMIC ASSETS - NO CACHE
  location / {
    proxy_pass http://pryzki;
  }
}

upstream fizkult {
  server fizkult_app:3004;
}

# деньфизкультурника2022.рф

server {
  listen 80;
	listen [::]:80;
  server_name деньфизкультурника2022.рф;
  return 301 https://деньфизкультурника2022.рф$request_uri;
}

server {
  listen 443 ssl;
  listen [::]:443 ssl;
  server_name деньфизкультурника2022.рф;
  ssl_certificate /etc/nginx/certs/www.деньфизкультурника2022.рф.crt;
  ssl_certificate_key /etc/nginx/certs/www.деньфизкультурника2022.рф.key;

  server_tokens off;
  
  gzip on;
  gzip_proxied any;
  gzip_comp_level 4;
  gzip_types text/css application/javascript image/svg+xml;

  proxy_http_version 1.1;
  proxy_set_header Upgrade $http_upgrade;
  proxy_set_header Connection 'upgrade';
  proxy_set_header Host $host;
  proxy_cache_bypass $http_upgrade;

  # BUILT ASSETS (E.G. JS BUNDLES)
  # Browser cache - max cache headers from Next.js as build id in url
  # Server cache - valid forever (cleared after cache "inactive" period)
  location /_next/static {
    proxy_cache STATIC;
    proxy_pass http://fizkult;
  }

  # STATIC ASSETS (E.G. IMAGES)
  # Browser cache - "no-cache" headers from Next.js as no build id in url
  # Server cache - refresh regularly in case of changes
  location /static {
    proxy_cache STATIC;
    proxy_ignore_headers Cache-Control;
    proxy_cache_valid 60m;
    proxy_pass http://fizkult;
  }

  # DYNAMIC ASSETS - NO CACHE
  location / {
    proxy_pass http://fizkult;
  }
}